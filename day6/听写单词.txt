前端模块的规范
后端模块规范
AMD规范和CMD规范的区别
AMD规范的代表产品是
CMD规范的代表产品是
requirejs的主模块用什么属性设置
require的常用api里哪个是设置配置的
require配置项里哪个属性是设置根路径的，并且是值是什么类型的
require配置项里哪个属性是设置模块路径映射的，并且是什么类型的
require配置项里哪个属性是加载非AMD规范模块的，并且是什么类型的，设置依赖的属性和映射的模块名称的属性是
require的常用api里那个函数式用来定义模块的，

动画库的构造函数的名称是
第一个参数是干什么的
第二个参数是干什么的
第三个参数是干什么的


动画持续的时间用什么属性设置
延迟动画的属性是
动画开始前调用的函数是
动画结束后调用的函数是
动画重复执行的属性
设置动画移动速度的样式

/***********************************************************************/

定义一个数组操作的模块：
	抛出方法getStr的方法，功能是返回数组里的所有字符串元素
	抛出方法getNum的方法，功能是返回数组里的所有数字元素
	抛出方法getMaxNum的方法，功能是返回数组里的最大的数字，没有的话返回null
	抛出方法getMinNum的方法，功能是返回数组里的最小的数字，没有的话返回null

定义一个DOM操作插件：
	抛出方法render的方法，功能是在指定的元素里渲染结构
	抛出方法renderList的方法，功能是在指定的元素里渲染数组里的数据
	抛出方法$的方法,返回匹配css选择器的元素


定义一个数据模块
	模拟一个二级菜单的数据

